{
  "name": "outlook-ai-draft-server",
  "version": "1.0.0",
  "description": "Backend proxy for the Outlook AI Draft add-in. Keeps the OpenRouter API key server-side.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
